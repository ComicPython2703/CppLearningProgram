
电脑:
硬件 --->  操作系统  ---> 程序(c语言、汇编语言、python、C++、Rust、Go、Java、C#、PHP、JavaScript等等)

单片机/嵌入式(大部分没有操作系统)
硬件 ---> 程序 (c语言、汇编语言、C++) 某些例外可能自带一些解释器,ESP32自带python解释器



常见操作系统:

传统桌面/服务器操作系统
Windows、Linux、macOS、
移动/嵌入式操作系统
Android(安卓)、HarmonyOS(鸿蒙)、Linux、

unix 祖宗 经过各种改进后(Linux、macOS、安卓、鸿蒙)

Linux也有很多分支:ubuntu、CentOS、Manjaro等等



所有程序都要加载，都要经过内存(或内存级别的可执行介质), 才能开始运行.
即使是GPU程序,也需要控制,也一定会一部分代码需要加载到内存中.


当内存空间不足时:拿硬盘当内存使用,但读写速度会急剧下降
硬盘（持久存储）→ 内存（运行时）→ CPU(执行)
    ↑_______________|
    虚拟内存机制（按需加载）

动态加载:程序在运行时按需从硬盘读取数据。



内存模型分为5个区:栈区、堆区、静态区、常量区、代码区。

最快->最慢
寄存器 > 内存(栈区 > 代码区 ≈ 常量区 > 静态区 > 堆区) > 硬盘


时间单位	相对长度
1 秒	眨一次眼（约 0.3 秒）
1 毫秒(ms)	1/1000 秒，人眼能感知
1 微秒(μs)	1/1000 毫秒，声音传播 0.34 毫米
1 纳秒(ns)	1/1000 微秒，光只走 30 厘米

在 1 纳秒内:
光速: 30 厘米
3GHz CPU: 完成 3 个指令周期
电信号:在芯片上走约 15 厘米

光速 = 299,792,458 米/秒 ≈ 30万公里/秒

区域	
栈区	纳秒级（移动指针）	 ~1 ns
代码区	编译时已定	        ~1-2 ns
常量区	编译时已定	        ~1-2 ns
静态区	编译时已定	        ~2-3 ns
堆区	微秒级(malloc复杂)	~3-5 ns(命中)


每个区存储的内容如下:

1、栈区:存放函数的参数值、局部变量等,由编译器自动分配和释放,通常在函数执行完后就释放了,其操作方式类似于数据结构中的栈。
栈内存分配由CPU的指令集负责,效率很高,但是分配的内存量有限,比如iOS中栈区的大小是2M。

2、堆区:若是通过new、malloc、realloc分配的内存块,编译器不会负责它们的释放工作,需要用程序区释放。
分配方式类似于数据结构中的链表。“内存泄漏”通常说的就是堆区。
(最常用)也可以让栈区帮忙自动释放,可以用作用域的方式来释放类、函数等。出作用域就自动释放堆区内存。

3、静态区:全局变量和静态变量的存储是放在一块的,初始化的全局变量和静态变量在一块区域,未初始化的全局变量和未初始化的静态变量在相邻的另一块区域。
程序结束后,由系统释放。

4、常量区:常量存储在这里,不允许修改。

5、代码区:顾名思义,存放代码。


高地址
+------------------+
|     栈区         |  ← 存放:局部变量、函数参数、函数返回值-隐藏参数（返回大对象时）、产生的临时对象、异常处理信息、栈帧指针（链接上下栈帧）
|                  |    每次调用函数时分配.返回时释放
|   main()的局部变量|   栈区存储的是函数调用所需的所有上下文信息！
|   add()的参数     |
|   add()的局部变量 |
+------------------+
|                  |
|     堆区         |  ← 存放: new/malloc 分配的内存
|                  |    需要手动释放  内存多大,堆就有多大
+------------------+
|                  |
|    静态区        |  ← 存放:全局变量、静态变量
|                  |    程序启动时分配.结束时释放 几十MB~几百MB
+------------------+
|                  |
|    常量区        |  ← 存放:字符串常量等
|                  |    只读.不可修改 几MB
+------------------+
|                  |
|    代码区         |  ← 存放:函数指令（二进制代码）
|                  |    只读.程序加载时确定
|  add()函数的指令  |
|  multiply()指令   |
|  main()函数的指令 |
+------------------+
低地址


提问: 什么叫低地址、高地址
内存就像一栋大楼:
- 低地址 = 1楼、2楼(小门牌号)
- 高地址 = 99楼、100楼(大门牌号)

地址: 0x0000  →  0x1000  →  0x2000  →  0xFFFF
      (低地址)                    (高地址)

内存区域	        存储内容	            特点	             生命周期

栈区	       局部变量、函数参数	     自动分配释放	        函数调用期间
堆区（传统）	new/malloc 分配	          手动管理	          从 new 到 delete
堆区（现代）  智能指针管理/栈区帮忙管理	    自动管理	        出作用域自动释放
静态区	       全局变量、静态变量	     程序启动时分配	       整个程序运行期间
常量区	      字符串常量、const常量	        只读	          整个程序运行期间
代码区	      函数指令、二进制代码	      只读、固定	       整个程序运行期间


提问: 用 const 修饰后的变量 会放入常量区吗？
答: 分2种情况
1、全局const → 放常量区 (只读)
2、局部const → 放栈区 (局部-函数内)


栈区和堆区大小差异？

栈区:栈区是向低地址扩展的,是一块连续的内存的区域。
栈顶的地址和栈的最大容量是系统预先规定好的,大小在进程分配时是确定的,具体大小看编译器,操作系统。
所需大小一般小于10M, 太大没有意义,不符合栈是用来快速存取的目标。

堆区:堆区是向高地址扩展的,是不连续的内存区域（这是由于系统是用链表来存储的空闲内存地址的,自然是不连续的是动态分配的）,
堆区的底层一般是链表实现的,在释放的时候很可能会将原本连续的内存断掉.

栈区和堆区效率差异？

栈区:由系统自动分配,速度较快。但程序员是无法控制的。(只要栈的剩余空间大于所申请空间,系统将为程序提供内存,否则将报异常提示栈溢出。)

堆区: 可以是由new分配的内存,一般速度比较慢,而且容易产生内存碎片, 。也可以不使用new,直接用栈帮忙,利用作用域释放堆区内存.
（首先应该知道操作系统有一个记录空闲内存地址的链表,当系统收到程序的申请时,会遍历该链表,
寻找第一个空间大于所申请空间的堆结点,然后将该结点从空闲结点链表中删除,并将该结点的空间分配给程序,
另外,对于大多数系统,会在这块内存空间中的首地址处记录本次分配的大小,这样,代码中的 delete语句才能正确的释放本内存空间。
由于找到的堆结点的大小不一定正好等于申请的大小,系统会自动的将多余的那部分重新放入空闲链表中）

小结:如果存放在堆中的数据如果不进行释放,程序必然造成内存泄漏,而且C++没有gc机制回收。所以对于堆中的内存使用,我们要记得用完释放。


原生数组作为参数时.如果指定了大小.整个数组会在栈上分配.超过几MB就会栈溢出崩溃!永远用指针或vector代替

存储方式只有顺序存储和链式存储两种.但是却可以组合成多种数据结构。

常见的数据结构:数组、链表、队列、栈、哈希表、二叉树、堆、图、

常见的算法:搜索、排序、分治、回溯、动态规划、贪心、


危险操作示例:
// 1. 不要在栈上定义大数组
void func() {
    int big[10'000'000];  // 崩溃 栈会炸
}

// 2. 不要用定长数组作为大参数
void process(int data[1000000]) {  // 危险 栈会炸
}

// 3. 不要返回局部数组地址
int* bad() {
    int arr[100];
    return arr;  // 危险
}
// 函数结束，arr 被销毁！在函数外使用索引无法访问,因为被销毁了.

提问:实例化对象或者函数用完了,想要释放内存,有什么方法?
答: 根据你创建的方式,分4种情况
1、如果是栈对象:不用管，出作用域自动释放。想提前释放就用 {} 包围
2、如果是堆对象(new):必须 delete
3、如果是容器/智能指针:不用管，自动释放
4、如果你的类有原生数组指针:在析构函数里写 delete[]，就会自动释放
